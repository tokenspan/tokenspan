query PaginateModelsQuery($args: ModelArgs!) {
  models(args: $args) {
    nodes {
      id
      name
    }
    totalNodes
    pageInfo {
      hasPreviousPage
      hasNextPage
      startCursor
      endCursor
    }
  }
}