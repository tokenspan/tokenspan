query GetProvidersQuery($args: ProviderArgs!) {
  providers(args: $args) {
    nodes {
      id
      name
      slug
      createdAt
      updatedAt
    }
    totalNodes
    pageInfo {
      endCursor
      startCursor
      hasNextPage
      hasPreviousPage
    }
  }
}