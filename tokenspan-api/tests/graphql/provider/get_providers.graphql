query GetProvidersQuery {
  providers {
    nodes {
      id
      name
      slug
      createdAt
      updatedAt
    }
    pageInfo {
      endCursor
      startCursor
      hasNextPage
      hasPreviousPage
    }
  }
}