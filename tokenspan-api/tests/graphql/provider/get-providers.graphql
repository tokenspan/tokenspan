query GetProvidersQuery {
  providers {
    edges {
      node {
        id
        name
        slug
        createdAt
        updatedAt
      }
      cursor
    }
    pageInfo {
      endCursor
      startCursor
      hasNextPage
      hasPreviousPage
    }
  }
}