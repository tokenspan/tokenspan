query PaginateProvidersQuery($args: ProviderArgs!) {
  providers(args: $args) {
    nodes {
      name
    }
    totalNodes
    pageInfo {
      endCursor
      startCursor
      hasNextPage
      hasPreviousPage
    }
  }
}